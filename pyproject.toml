[project]
name = "orthos"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "numba",
]

[project.optional-dependencies]
gpu = ["cupy-cuda12x"]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
